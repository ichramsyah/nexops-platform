---
# playbooks/04-postgresql.yml
# Installs and configures standalone PostgreSQL on nexops-db VM
- name: Configure Standalone PostgreSQL Database VM
  hosts: db
  become: true
  gather_facts: true

  roles:
    - role: postgresql
      tags: [postgresql, database]
